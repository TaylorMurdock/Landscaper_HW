// LANDSCAPER

let wallet = 0;

const cutGrass = () => {
    wallet += 1;
    console.log(`You just made $1 for cutting that yard! The total amount in your wallet is now $${wallet}.`)
}

cutGrass();
cutGrass();
cutGrass();